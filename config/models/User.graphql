# Modelo/Schema 
type Profile {
  id: Int
  name: String	
}

enum UserStatus {
  ACTIVE
  INACTIVE
  LOCKED
}

type User {
  id: Int
  name: String!
  email: String!
  age: Int
  profile: Profile
  status: UserStatus
}